"""Infrastructure services (mirror of teacher's src/infrastructure/services/).

Responsibility: adapters for third-party / external services (email, storage,
notifications, payment gateways, etc.). These wrap libraries outside the app
and are injected into domain / application services via dependency injection.

NOTE: empty — no external service implemented yet.
"""
